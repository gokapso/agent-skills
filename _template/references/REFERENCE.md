# Reference

## Contents

- Overview
- Inputs and environment
- API endpoints
- Error handling
- Examples

## Overview

Put longer docs here (playbooks, endpoint catalogs, gotchas). Keep `SKILL.md` short and point here as needed.

## Inputs and environment

Expected env vars:

- `KAPSO_API_BASE_URL` (root, e.g. `https://api.kapso.ai`)
- `KAPSO_API_KEY`
- `PROJECT_ID`
- `KAPSO_META_BASE_URL` (optional, defaults to `${KAPSO_API_BASE_URL}/meta`)

## API endpoints

Document the endpoints the scripts call and what they do.

## Error handling

Define your error JSON schema and common remediation steps.

## Examples

Include real input/output examples.
